# Ignore large Claude Code project session files
.claude/projects/

# Ignore debug files
.claude/debug/

# Ignore credentials
.claude/.credentials.json

# Ignore settings (managed by Claude Code)
.claude/settings.json

# Ignore IDE lock files and directory
.claude/ide/

# Ignore file history cache
.claude/file-history/

# Ignore session environment
.claude/session-env/

# Ignore shell snapshots
.claude/shell-snapshots/

# Ignore analytics/telemetry
.claude/statsig/

# Ignore security warnings state
.claude/security_warnings_state_*.json

# Ignore history file
.claude/history.jsonl

# Ignore Claude MCP state
.claude/mcp.json

# Ignore plugin .git directories (can be large)
.claude/plugins/marketplaces/**/.git/

# Ignore vim plugins and cache (managed by Vundle)
.vim/

# Ignore tmux plugins (managed by TPM)
.tmux/plugins/

# Ignore Claude Code plugin marketplaces (managed by Claude Code)
.claude/plugins/marketplaces/

# Ignore auto-generated fish shell configuration files
.config/fish/conf.d/omf.fish
.config/fish/conf.d/rustup.fish

# Nix-managed dotfiles (Phase 4 migration)
# Note: Fish shell remains managed by chezmoi (has encrypted files)
# Will migrate to sops-nix in Phase 5

# Starship prompt (managed by home-manager programs.starship)
.config/starship.toml

# Tmux config (managed by home-manager programs.tmux)
.tmux.conf
.config/tmux

# Vim config (managed by home-manager programs.vim)
.vimrc
.vimrc.d

# Alacritty config (managed by home-manager programs.alacritty)
.config/alacritty

# Fish shell config (managed by home-manager programs.fish)
# Abbreviations sourced from sops-nix secret at runtime
.config/fish

# Atuin config (managed by home-manager programs.atuin)
.config/atuin

# Direnv config (managed by home-manager programs.direnv)
.config/direnv

# K9s config (managed by home-manager + sops-nix)
# Note: k9s/config.yaml is a sops secret, aliases/plugins/views are declarative
.config/k9s

# Exercism config (managed by home-manager + sops-nix)
# Note: user.json contains API token, handled via sops secret
.config/exercism

# Mise config (managed by home-manager programs.mise)
.config/mise

# Broot config (managed by home-manager programs.broot)
.config/broot

# Grype config (managed by home-manager xdg.configFile)
.config/grype

# Syft config (managed by home-manager xdg.configFile)
.config/syft

# Tmuxp config (managed by home-manager xdg.configFile)
.config/tmuxp

# Lnav config (managed by home-manager home.file)
.lnav

# Bash config (managed by home-manager programs.bash)
.bashrc
.profile
.bash_profile

# Claude Code config (managed by home-manager + sops-nix)
# CLAUDE.md, installed_plugins.json, known_marketplaces.json are sops secrets
.claude/CLAUDE.md
.claude/installed_plugins.json
.claude/known_marketplaces.json
